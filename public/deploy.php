<?php
	include("../auto-deploy.php");

// for Linux
//$result = shell_exec('git pull');

//echo "<pre>$result</pre>";

// Run commands inside the container
//$response = shell_exec('docker exec -it transfer_app_1 bash -c "composer install; php artisan migrate; exit"');

// Print response
//echo "<pre>$response</pre>";

